(() => {
  'use strict';
  const presets={tiktok:{name:'TikTok',width:1080,height:1920,zones:[{cls:'safe-top',label:'KEEP CLEAR'},{cls:'safe-right',label:'UI'},{cls:'safe-bottom',label:'CAPTION / NAV'}]},reels:{name:'Instagram Reels',width:1080,height:1920,zones:[{cls:'safe-top',label:'KEEP CLEAR'},{cls:'safe-right',label:'UI'},{cls:'safe-bottom',label:'CAPTION / NAV'}]},shorts:{name:'YouTube Shorts',width:1080,height:1920,zones:[{cls:'safe-top',label:'KEEP CLEAR'},{cls:'safe-right',label:'UI'},{cls:'safe-bottom',label:'TITLE / NAV'}]}};
  const input=document.getElementById('safeFileInput'),drop=document.getElementById('safeDropZone'),info=document.getElementById('safeFileInfo'),select=document.getElementById('safePreset'),stage=document.getElementById('safeStage'),img=document.getElementById('safePreview'),overlay=document.getElementById('safeOverlay'),status=document.getElementById('safeStatus');
  let url=null;
  function renderOverlay(){const p=presets[select.value];overlay.innerHTML=`<div class="safe-boundary"><span>SAFE CONTENT AREA</span></div>${p.zones.map(z=>`<div class="unsafe-zone ${z.cls}"><span>${z.label}</span></div>`).join('')}`;status.textContent=`${p.name} · ${p.width} × ${p.height} preview`;}
  function setFile(file){if(!file||!file.type.startsWith('image/'))return alert('Please choose an image file.');if(url)URL.revokeObjectURL(url);url=URL.createObjectURL(file);img.onload=()=>{stage.hidden=false;info.hidden=false;info.textContent=`${file.name} · ${(file.size/1024/1024).toFixed(2)} MB`;renderOverlay();};img.onerror=()=>alert('Could not read this image.');img.src=url;}
  input.addEventListener('change',e=>setFile(e.target.files[0]));select.addEventListener('change',renderOverlay);['dragenter','dragover'].forEach(t=>drop.addEventListener(t,e=>{e.preventDefault();drop.classList.add('dragging')}));['dragleave','drop'].forEach(t=>drop.addEventListener(t,e=>{e.preventDefault();drop.classList.remove('dragging')}));drop.addEventListener('drop',e=>setFile(e.dataTransfer.files[0]));renderOverlay();
})();