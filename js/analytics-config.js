/* Public tracking configuration. Fill from the owner's Umami tracking snippet.
 * Empty values keep the integration completely disabled. Never put API keys here. */
window.DroopAnalyticsConfig=Object.freeze({websiteId:'',scriptUrl:''});
