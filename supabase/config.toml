[functions.lemonsqueezy-webhook]
verify_jwt = false

[functions.lemonsqueezy-checkout]
# The handler performs its own user JWT verification against Supabase Auth.
verify_jwt = false

[functions.lemonsqueezy-portal]
# The handler validates the signed-in Supabase user itself.
verify_jwt = false


[functions.paypal-webhook]
# External PayPal calls are verified inside the handler.
verify_jwt = false

[functions.paypal-checkout]
# The handler validates the signed-in Supabase user itself.
verify_jwt = false

[functions.paypal-subscription]
# The handler validates the signed-in Supabase user itself.
verify_jwt = false
