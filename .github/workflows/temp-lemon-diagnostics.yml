name: Temporary Lemon portal diagnostics

on:
  push:
    branches:
      - debug/lemon-diagnostics

permissions:
  contents: read

jobs:
  diag:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Inspect subscription portal fields
        run: curl -fsSL https://qbzqiiinugidkdxcpdln.supabase.co/functions/v1/billing-diagnostics-temp
